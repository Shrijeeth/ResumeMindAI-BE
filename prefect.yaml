name: resumemind-ai

deployments:
  - name: health-6h
    entrypoint: flows/health.py:health_check
    work_pool:
      name: default
    schedule:
      interval: 21600  # 6 hours in seconds
    parameters: {}
